API_UPSTREAM=https://managed-agent.chiefaiofficer.id
API_UPSTREAM_HOST=managed-agent.chiefaiofficer.id

# Optional temporary compatibility with the current frontend auth contract.
# Do not use an admin/sensitive key here for public production.
VITE_API_KEY=
